source "https://rubygems.org"

# Ruby 3.2+ 已去掉 String#untaint；Gemfile.lock 的 BUNDLED WITH 须为 Bundler 2.x（1.17.x 会崩溃）
# 若在终端直接 bundle exec 命中 /usr/bin/bundle（Ruby 2.6）报错，请用：./scripts/fastlane.sh <lane>
gem "fastlane", ">= 2.220.0"
